create Table users(
	user_id Varchar PRIMARY KEY,
	name Varchar(100) NOT NULL,
	email Varchar(50) NOT NULL UNIQUE,
	password Varchar(20) NOT NULL,
	role Varchar NOT NULL,
	is_profile_completed Boolean Default FALSE,
	created_at TIMESTAMP Default CURRENT_TIMESTAMP,
	updated_at TIMESTAMP Default CURRENT_TIMESTAMP
);

select * from users;

delete from users where email='abc@example.com';

alter table users
alter column is_profile_completed SET NOT NULL;

Create Table jobseekers(
	user_id VARCHAR PRIMARY KEY,
	full_name VARCHAR(30) NOT NULL,
	profile_picture VARCHAR(100),
	year_of_experience VARCHAR(20) NOT NULL,
	bio TEXT,
	preferred_roles VARCHAR(100) NOT NULL,
	created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	CONSTRAI
);

select * from jobseekers;

alter Table jobseekers
Drop COLUMN preferred_roles;


CREATE TABLE preferred_roles (
    user_id VARCHAR NOT NULL,
    role VARCHAR(100) NOT NULL,
    PRIMARY KEY (user_id, role),
    CONSTRAINT fk_user
      FOREIGN KEY (user_id)
      REFERENCES jobseekers(user_id)
      ON DELETE CASCADE
);

select * from preferred_roles;

delete from jobseekers where user_id = 'a660d452-4024-4490-8a1e-990fdfdcaf57';


CREATE TABLE RECRUITER(
	user_id VARCHAR PRIMARY KEY,
	full_name VARCHAR(30) NOT NULL,
	profile_picture VARCHAR(100),
	company_name VARCHAR(50) NOT NULL,
	job_title VARCHAR(50) NOT NULL,
	company_size VARCHAR(20) NOT NULL,
	industry VARCHAR(50) NOT NULL,
	created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT fk_recruiter
	FOREIGN KEY(user_id)
	REFERENCES users(user_id)
	ON DELETE CASCADE
);

select * from recruiter;

CREATE TABLE Mentors(
	user_id VARCHAR PRIMARY KEY,
	full_name VARCHAR(30) NOT NULL,
	profile_picture VARCHAR(100),
	year_of_mentoring VARCHAR(20) NOT NULL,
	bio TEXT,
	created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
	CONSTRAINT fk_mentor
	FOREIGN KEY(user_id)
	REFERENCES users(user_id)
	ON DELETE CASCADE
);

CREATE TABLE expertise (
    user_id VARCHAR NOT NULL,
    field VARCHAR(100) NOT NULL,
    PRIMARY KEY (user_id, field),
    CONSTRAINT fk_user
      FOREIGN KEY (user_id)
      REFERENCES jobseekers(user_id)
      ON DELETE CASCADE
);

select * from mentors;

alter Table expertise
Drop CONSTRAINT fk_user;

alter Table expertise
ADD CONSTRAINT fk_user
FOREIGN KEY (user_id)
      REFERENCES mentors(user_id)
      ON DELETE CASCADE;

select * from expertise;

create Table resumes(
user_id Varchar Not Null,
file_name Varchar NOT NULL,
resume_content jsonb NOT NULL,
created_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
updated_at TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP,
CONSTRAINT pk_resume
PRIMARY KEY(user_id,fil)
);

select * from resumes;

select * from resumes where user_id = 'a660d452-4024-4490-8a1e-990fdfdcaf57';

delete from users where user_id = 'bf299f92-bb6d-4db4-b0db-a2939c20dc08';

create Table apilogs(
	log_id SERIAL PRIMARY KEY,
	user_id VARCHAR,
	api_hit_name Varchar NOT NULL,
	api_hit_response VARCHAR NOT NULL,
	api_hit_date DATE DEFAULT CURRENT_DATE NOT NULL,
	api_hit_time TIME DEFAULT CURRENT_TIME NOT NULL
);

SELECT * FROM apilogs;




